const { default: test } = require('node:test')
const { describe } = require('yargs')
const LbbtCalculator = require('../LBBT-calculator.js')

describe('tests for the LBBT calculator', () => {
    test('', () => {
        
    })
})